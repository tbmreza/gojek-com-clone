<script>
  import Icon from "svelte-awesome";
  import { arrowRight } from "svelte-awesome/icons";

  const rule = `
    color: var(--brand-main);
  `;
</script>

<Icon data={arrowRight} scale="1" style={rule} />
